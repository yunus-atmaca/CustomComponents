import {moderateScale} from 'react-native-size-matters';

export const TAB_BAR_HEIGHT = moderateScale(48);
export const HEADER_HEIGHT = moderateScale(56);
export const APP_PIP_OFF = moderateScale(24);
